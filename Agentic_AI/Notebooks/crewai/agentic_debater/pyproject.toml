[project]
name = "agentic_debater"
version = "0.1.0"
description = "agentic-debater using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.13"
dependencies = [
    "crewai[tools]>=0.121.1,<1.0.0"
]

[project.scripts]
agentic_debater = "agentic_debater.main:run"
run_crew = "agentic_debater.main:run"
train = "agentic_debater.main:train"
replay = "agentic_debater.main:replay"
test = "agentic_debater.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
