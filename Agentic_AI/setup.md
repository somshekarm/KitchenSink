1. `uv` is package manager and it is fast and reliable. Install `uv` in stand alone mode.
2. From here, https://docs.astral.sh/uv/getting-started/installation/
3. After installation, start running `uv self update` to make sure you are on latest
4. If you have used Anaconda, de-activate it, then run `uv sync` to sync packages
